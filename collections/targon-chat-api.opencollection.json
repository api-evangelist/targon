{
  "opencollection": "1.0.0",
  "info": {
    "name": "Targon Chat API",
    "version": "1.0"
  },
  "request": {
    "auth": {
      "type": "bearer",
      "token": "{{bearerToken}}"
    }
  },
  "items": [
    {
      "info": {
        "name": "Chat",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "Create a chat completion",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "https://api.targon.com/v1/chat/completions",
            "body": {
              "type": "json",
              "data": "{}"
            }
          },
          "docs": "Generates a model response for the given chat conversation. Compatible with the OpenAI Chat Completions API. Set `stream: true` to receive the response as Server-Sent Events."
        }
      ]
    }
  ],
  "bundled": true
}